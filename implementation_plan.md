# Add Diabetes Prediction Project

The goal is to add a new project entry "Diabetes Prediction" to the portfolio's project section. This entry will include a "Demo" button linking to a specific URL.

## Proposed Changes

### Portfolio Content

#### [MODIFY] [index.html](file:///e:/ishva.works/Portfolio_New/index.html)
- Add a new `.project-card` to the `.projects-grid` section.
- **Project Details:**
    - **Title:** Diabetes Prediction
    - **Icon:** `fa-notes-medical` or `fa-heartbeat` (Health related)
    - **Tags:** Python, Machine Learning, Data Analysis
    - **Description:** Analytical model to predict diabetes likelihood based on diagnostic measurements.
    - **Links:**
        - Keep the "View Code" GitHub icon (generic `#` for now or the demo link if repo is same, but probably separate).
        - **NEW:** Add a "Demo" button/link pointing to `https://ishva24.github.io/Diabetes_Prediction/`.

## Verification Plan

### Manual Verification
1. Open `index.html` in a browser (or preview).
2. Navigate to the "Projects" section.
3. Verify the "Diabetes Prediction" card exists.
4. Click the "Demo" button.
5. Verify it opens `https://ishva24.github.io/Diabetes_Prediction/`.
6. visually check that the existing layout of the grid is maintained.
